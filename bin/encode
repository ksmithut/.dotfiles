#!/usr/bin/env node
'use strict'

const args = process.argv.slice(2)

const base64encode = (val) => new Buffer(val || '').toString('base64')

process.stdout.write(base64encode(args[0]))
process.stderr.write('\n')
