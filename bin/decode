#!/usr/bin/env node
'use strict'

const args = process.argv.slice(2)

const base64decode = (val) => new Buffer(val || '', 'base64').toString()

process.stdout.write(base64decode(args[0]))
process.stderr.write('\n')
