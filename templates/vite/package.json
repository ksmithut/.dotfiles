{
  "name": "unnamed",
  "description": "",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "format": "prettier --write .",
    "lint": "prettier --check . && tsc"
  }
}
