#!/usr/bin/env bash

n latest
n stable

mkdir ~/.npm
sudo chown -R `whoami` ~/.npm
sudo chown -R `whoami` /usr/local/lib/node_modules

heroku login

npm install -g npm travis-encrypt slush mocha jshint gulp grunt-cli bower
